<nav class="navbar navbar-expand-lg text-white">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main_home') }}"><img src="{{ asset('img/logo.svg') }}" alt="Series"></a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="{{ path('main_home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('serie_list') }}">TV Shows</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('serie_add') }}">New TV Show</a>
                </li>
                    <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_logout') }}">Logout ({{ app.user.firstname }})</a>
                </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_register') }}">Register</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_login') }}">Login</a>
                </li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>